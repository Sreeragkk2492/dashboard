import 'package:flutter/material.dart';

class TablestLogin extends StatelessWidget {
  const TablestLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}